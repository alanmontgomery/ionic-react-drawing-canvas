import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionicreactdrawing.app',
  appName: 'ionic-react-drawing-canvas',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
